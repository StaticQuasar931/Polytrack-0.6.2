# Third-Party Notices

## Bootstrap Icons

The following assets come from [Bootstrap Icons](https://github.com/twbs/icons) and are licensed under the MIT License:

- `images/ranked-medal.svg` uses `award-fill`.
- `images/ranked-trophy.svg` uses `trophy-fill`.
- `images/ranked-award.svg` uses `award-fill`.
- `images/ranked-star.svg` uses `star-fill`.

The complete license text is stored in `licenses/bootstrap-icons-LICENSE.txt`.
